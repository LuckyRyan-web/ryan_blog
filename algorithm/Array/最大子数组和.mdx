---
title: 53. 最大子数组和
categories: 算法
tags:
    - 动态规划
---

[leetcode 地址](https://leetcode.cn/problems/maximum-subarray/)

## 解法一：动态规划

```js
/**
 * @param {number[]} nums
 * @return {number}
 */
var maxSubArray = function (nums) {
    let pre = 0
    let sum = nums[0]

    nums.forEach((v) => {
        pre = Math.max(v, v + pre)

        sum = Math.max(pre, sum)
    })

    return sum
}
```
